---
language: node_js
node_js:
  - 11
  - 10
  - 8
cache:
  directories:
    - ~/.npm
before_install:
  - npm install -g npm@latest
install:
  - npm install
script:
  - npx lerna bootstrap --no-ci
  - npx lerna run test
  - npx commitlint-travis

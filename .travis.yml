---
language: node_js
node_js:
  - 11
  - 10
  - 8
cache:
  directories:
    - ~/.npm
before_install:
  - npm install -g npm@latest
install:
  - npm ci
script:
  - npx lerna bootstrap
  - npx lerna run test
  - npx commitlint-travis
